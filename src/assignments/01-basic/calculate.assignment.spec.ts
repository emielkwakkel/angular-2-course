import { incrementPositiveNumber } from './calculate';

describe('calculate', () => {
    it('should increment a positive input number by one', () => {
        // TODO call the incrementPositiveNumber with a positive input
        const result = 11;

        expect(result).toBe(11);
    });

    it('should return zero if negative input number is supplied', () => {
        // TODO test if a zero is returned when a negative input number is supplied.
    })
});
